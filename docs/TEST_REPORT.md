# 0.1.1-alpha 验证更新

日期：2026-09-25。

- 20 项 JVM 测试通过：新增名称优先级、按地址合并、系统记录与广播证据分离、列表优先级、未命名广播上限、手动地址格式、同名设备隔离及乱序观测检查。
- Android 15 模拟器 7 项测试通过，0 失败、0 跳过：新增没有广播时选择系统记录、未命名项默认折叠、重选原设备保留位置。
- Lint 无错误。设备选择界面采用测试注入的模拟系统记录核对；不将这些数据作为小米手环 11 真机兼容证据。
- 发布使用与 0.1.0 相同的应用 ID 和签名，versionCode 从 1 升为 2；可覆盖安装。
- 实机能否返回具体手环的配对/连接记录，以及选中后能否持续收到广播，仍需实测。不能读取小米运动健康私有绑定记录。

下方保留首版验证基线。

# 0.1.0-alpha 验证报告

日期：2026-09-24。

## 已完成

| 检查 | 结果 |
|---|---|
| JDK 17 / Gradle 8.11.1 / AGP 8.9.2 / SDK 35 构建 | Debug、Release、测试 APK 构建成功 |
| JVM 单元测试 | 12 项通过，0 失败 |
| Android 15 / API 35 ARM64 模拟器测试 | 5 项通过，0 失败、0 跳过 |
| Android Lint | 0 错误；保留中文单语言文本和测试库更新提示 |
| Release APK 签名 | APK Signature Scheme v2 校验通过；RSA 3072 |
| 实际签名包安装与启动 | 模拟器安装成功，冷启动成功，无 crash buffer 错误 |
| 界面检查 | 实际截图检查首页和设置；空状态地图按钮禁用，布局无横向溢出 |
| GitHub Actions | 自动构建、逻辑测试、Lint、设备测试与截图流水线已配置 |

签名证书 SHA256：`f579e99f2d87fd3c1c44bb2037d370cd126ea8c5cd2b1262027d416fa53cc13b`。

APK 的最终大小及 SHA256 以 Release 附件和 `SHA256SUMS.txt` 为准。签名私钥只保存在维护者本机，不在源码仓库中。

## 测试覆盖

- 从未见过设备不触发丢失；阈值边界；单次失联只提醒一次；重见恢复；权限/蓝牙错误与主动暂停不触发丢失。
- 乱序扫描结果不能回退最后观测时刻；新的守护会话不能将旧会话历史当作在线证据。
- 拒绝过期、发生在观测之后、精度无效或经纬度无效的定位；拒绝缓存过久或来自未来的扫描结果。
- 新观测没有位置时，保留此前有效位置及其自己的时间；诊断不导出坐标和设备身份；事件记录最多 100 条。
- 原生 Activity 页面、地图空状态禁用、设置弹窗。
- 用户从可见页面启动前台服务：正常进入等待首次观测；历史记录不误触发在线；暂停后释放服务并保留记录。

## 尚未验证，不作为已支持能力宣传

- 小米 17 + 小米手环 11 的真实广播和官方 App 共存。
- BLE 随机地址轮换、息屏 8 小时、10 次离开返回和真实提醒延迟。
- 高德/百度实地点位核对、室内定位与额外耗电。
- 其他 Android 厂商、最低 Android 10 实机、其他手环型号。

模拟器测试不证明手环硬件兼容。本版属于可安装的功能 Alpha，按地址被动观测；完整真机验收清单见 GETTING_STARTED.md。
