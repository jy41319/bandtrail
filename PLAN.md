# 手环最后位置助手：实施计划

日期：2026-09-24。当前阶段：规划，尚未验证手环 11 的实际 BLE 行为。

## 目标与范围

做一个面向普通用户的轻量开源 Android App。用户下载安装 APK、选择自己的手环并开启守护后，可以查看手机最后一次检测到手环时的手机位置；后续增加失联提醒与附近寻找。无需账户、服务器或用户部署环境。

用户目前使用小米 17 + 小米手环 11。小米 17 是首台验证设备，产品不绑定小米手机。首版以通用 Android API 实现，暂定 Android 10 及以上；兼容范围最终由真机测试决定。不同品牌的后台设置提供独立引导。iPhone 属于后续单独评估的版本，不在首版 APK 的交付范围内。

优先争取与小米运动健康共存，保留原有健康、运动及通知功能。其他手环型号通过适配接口逐步增加，不因同属 BLE 就宣称兼容。

## 必须明确的能力边界

- 保存的是手机检测到手环时的手机位置，可作为寻找起点；不是手环实时位置或准确掉落点。
- 必须提前开启守护；无法追溯安装前、关闭守护期间或未观测到设备期间的位置。
- 手环观测时间、定位采样时间、定位精度分别保存。旧定位显示其时间，不包装成刚刚取得的位置。
- 未检测到手环不等同于手环丢失；关闭蓝牙、撤销权限或监测中断显示为监测不可用。
- RSSI 只显示信号强弱和趋势，不换算为精确米数，不据此声称检测到脱落。

## 阶段 0：兼容性验证，决定技术路线

交付一个诊断 APK，记录目标设备观测、广播特征、RSSI、观测间隔和应用监测状态。诊断记录默认本地，用户主动导出时再脱敏。

在小米 17 + 手环 11 上测试：

1. 小米运动健康连接及未连接两种状态，能否发现并持续识别同一只手环。
2. 锁屏、息屏、充电、手环重启及重新连接后的广播与地址变化。
3. 多只同型号设备附近时是否会误认；设备名称不能作为唯一身份标识。
4. 离开范围、重新靠近时的观测延迟；守护是否影响官方 App 同步。
5. 不依赖 Google Play 服务时，系统定位的可用性和位置新鲜度。

路线选择：

- 广播可持续识别：采用有过滤条件的 BLE 扫描，研究 PendingIntent 扫描与前台服务的组合。
- 广播不足：验证系统关联设备存在性能力或 GATT 连接能否满足需求，并检查与官方 App 的共存情况。
- 若必须认证、接管连接或官方 App 不再正常工作：先形成可复现诊断结论，重新评估是否增加协议适配；不能将这种限制隐藏在产品里。

通过条件：官方 App 正常使用时可稳定归属目标手环，并能区别设备失联与本 App 监测失效。未通过前不承诺可靠防丢。

## 阶段 1：最后位置 MVP

完成“添加设备 → 开启守护 → 本地记录 → 查看最后位置 → 外部地图打开”的闭环。

- 首页：设备名称、最近检测时间、守护状态、最近有效位置及精度、地图入口。
- 设置：权限状态、守护开关、后台运行引导、删除本地记录。
- 数据：只保存目标设备必要观测和少量历史，不采集周围其他设备的轨迹。
- 定位：守护期间按移动情况调整更新频率，观测与定位按时间匹配；失联后新增的手机位置不能覆盖最后观测位置。
- 地图：优先外部地图 Intent；未安装地图时提供坐标复制。逐一验证地图所需坐标系，避免国内地图偏移。
- 未取得位置时明确显示“当时未获取到位置”，保留有效观测时间。

建议实现：Kotlin + Android Views + 系统 BLE/LocationManager + 本地 SQLite。基础功能不强依赖 Google Play 服务，无内置地图 SDK、账户或云后端。根据阶段 0 结果确定前台服务类型、权限和设备识别方案。

模块保持简单：设备适配、守护状态机、定位记录、本地存储、页面与地图跳转。业务逻辑不依赖具体手机品牌。

## 阶段 2：提醒与附近寻找

- 连续未检测到目标一段时间后提醒，重见后恢复；延迟阈值由阶段 0 的实际观测间隔校准。
- 区分最近检测到、疑似失联、监测不可用和主动暂停，避免将系统限制误报为丢失。
- 支持通知、可选震动/声音、临时静音与重复提醒抑制。
- 附近寻找由用户主动开启，短时间提高扫描频率，展示平滑后的 RSSI 趋势及数据更新时间。
- 让手环震动仅在确认协议支持后列入后续功能，不阻塞 MVP。

## 阶段 3：跨品牌测试与发布

至少覆盖小米 17、一台不同品牌 Android 手机，以及一组无 Google Play 服务的环境（可与前两者重合）。公开手机系统、手环型号、固件与实测能力矩阵。

验收场景：

- 息屏连续守护至少 8 小时，报告最大观测间隔、误报和有无监测空档。
- 至少 10 次离开与返回，记录发现率、提醒延迟及重见恢复情况。
- 蓝牙关闭、定位关闭、权限撤销、系统终止进程和手机重启时，状态正确；监测恢复遵循系统限制，需要用户操作时明确提示。
- 室内定位失败或位置陈旧时，页面不生成假位置；外部地图点位经人工核对。
- 应用退出重开后最后记录仍在；小米运动健康同步正常。
- 对照关闭守护的同类使用时段，测量额外耗电；根据结果调整定位与扫描策略。

交付源码、构建说明、测试报告、兼容列表、隐私说明、签名 APK 和校验值。APK 以不超过 10 MB 为优化目标，以实际 Release 构建结果为准。首发计划为 GitHub Releases，发布前检查开源依赖及许可证，妥善保管持续更新所需的签名密钥。

阶段 0 是后续排期的前置条件；优先产出诊断 APK，通过后再开发完整界面。开发可在电脑完成，手环广播、后台稳定性和耗电验证需要真机参与。

## 查证依据

- Android BLE 后台官方文档说明了扫描、PendingIntent、关联设备与长连接的可选方案和限制；不能假设每 5 秒运行一次后台循环就会稳定工作：https://developer.android.com/develop/connectivity/bluetooth/ble/background
- 前台定位服务有相应权限与启动约束：https://developer.android.com/develop/background-work/services/fgs/service-types
- Gadgetbridge 的 Xiaomi 设备页可作为协议研究入口。本次查阅未见手环 11 的明确支持条目，不能据旧型号推断支持：https://gadgetbridge.org/gadgets/wearables/xiaomi/
- iTag README 明确包含最后位置、失联提醒与 RSSI，可借鉴产品流程，不能视为手环 11 兼容证明：https://github.com/s4ysolutions/itag

以上为设计方案与待验证项，不代表已完成硬件适配或测试。

## 实施更新（2026-09-24）

0.1.0-alpha 已按本计划实现核心流程。为控制依赖，实际采用原生 Java + Android Views，少量状态用 SharedPreferences；尚未实现完整 SQLite 历史或 PendingIntent 恢复。硬件验证仍是发布为稳定版的前提。以 README 和 docs/ENGINEERING.md 的当前实现说明为准。
